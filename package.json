{
  "name": "kenmon-monorepo",
  "version": "0.0.3",
  "private": true,
  "description": "Portable authentication module - Monorepo",
  "author": "Junyoung Choi <rokt33r.choi@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "clean": "pnpm -r --filter='./packages/*' clean",
    "test": "pnpm -r --filter='./packages/*' test",
    "format": "pretty-quick --staged",
    "format:all": "prettier --write .",
    "dev:nextjs": "pnpm --filter nextjs dev"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "pretty-quick": "^4.2.2",
    "typescript": "^5.0.0"
  }
}
